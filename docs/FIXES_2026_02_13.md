# Fixes (2026-02-13)

- Added missing pivot table migration `invoice_delivery_note` to support invoice creation from delivery notes (BL).

## After update

Run:

```bash
php artisan migrate
php artisan optimize:clear
```
